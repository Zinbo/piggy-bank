angular.module('MyApp', []).controller('PiggyController', function($scope) {

	$scope.init = function() {
			//Get all items from database
			
			//populate money fields
			
			//populate tables
	}	
	
	$scope.addItem = function(item) {
		//add item to the database
		//add this to the tables and the money values
	
	}
}